name: mercurius-example
log_destination: ./reviews
default_budget: 4
max_findings: 6
review_context: |
  Deployment context and constraints go here. Suppress findings that do not
  materially affect readiness under this context.
prompt_overrides: |
  Focus on design clarity, missing acceptance criteria, and risks that would
  block a straightforward implementation.
reviewers:
  - name: codex
    impl: codex
    model: gpt-5.5
